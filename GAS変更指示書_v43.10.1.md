# GAS変更指示書 v43.10.1（パスワード保護）→ ✅ 確認済み・作業不要

**2026-07-04 追記: GAS本体（v157）のソースコードを直接確認した結果、本指示書の2つの変更は
どちらも既に実装済みであることを確認しました。GAS側の作業は不要です。**

## 確認結果の詳細

| 指示書の項目 | GAS v157 の実装状況 |
|---|---|
| 変更1: workers応答からパスワード列を除外 | ✅ 実装済み。`out_()` が全ロール向けの応答から `パスワード`・`パスワードハッシュ` を削除している（一般ロールはさらに `ログインID`・`標準単価` も除去） |
| 変更2: 更新時にパスワードが空なら既存を維持 | ✅ 実装済み。`normalizeWorkerPasswordData_()` が空・未指定のパスワードを無視し、既存値を維持する |
| （追加確認）保存形式 | ✅ v101 以降、パスワードは**ハッシュ化して保存**（平文は保存しない）。既存の平文はログイン成功時・編集保存時に自動でハッシュへ移行 |

## フロント側（v43.10.1で対応済み）との整合

- 管理画面のパスワード欄は「変更する場合のみ入力」方式（空欄なら変更なし）→ GASの「空なら維持」と正しく噛み合います
- 作業員一覧の端末キャッシュからパスワードを除外済み → そもそもGASが送ってこないため二重に安全です

## 残る運用チェック（任意）

古い時期に登録した作業員に**平文パスワードがシートに残っている**可能性があります。
`diagnosis/`（エラー診断）の「作業員ログイン設定」に「平文パスワード残り ○件」と表示されます。
残っている場合も、その作業員が一度ログインするか、管理画面で保存し直すと自動でハッシュ化され平文欄は空になります。

---

以下は当初の指示書（記録として保存）。**実施不要です。**

<details>
<summary>当初の指示内容（クリックで展開）</summary>

### 変更1: workers応答からパスワード列を除外する

```javascript
workers = workers.map(function(w){
  var c = {};
  for (var k in w) { if (k !== 'パスワード') c[k] = w[k]; }
  return c;
});
```

### 変更2: updateWorker で「パスワードが空・未指定なら変更しない」

```javascript
if (!data['パスワード']) {
  delete data['パスワード'];
}
```

</details>
