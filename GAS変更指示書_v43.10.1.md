# GAS変更指示書 v43.10.1（パスワード保護の完全対策）

対象: Google Apps Script `Code_v157.gs`(またはそれ以降)

## 背景

v43.10.1のフロント側修正で、管理画面は以下のようになりました。

- パスワード欄は画面に**表示しない**(変更する場合のみ入力。空欄なら変更なし)
- 作業員一覧の端末キャッシュ(localStorage)から**パスワードを除外**

ただし、GASの `workers` 応答に今もパスワード列が含まれているため、通信内容としてはブラウザに届いています。完全に保護するには、以下のGAS側変更が必要です。

## 変更1: workers応答からパスワード列を除外する

作業員一覧を返す処理(例: `action=workers` や `action=adminData` などで作業員マスタを返している箇所)で、各行のオブジェクトから「パスワード」キーを削除してから返してください。

```javascript
// 例: 作業員一覧を返す直前に追加
workers = workers.map(function(w){
  var c = {};
  for (var k in w) { if (k !== 'パスワード') c[k] = w[k]; }
  return c;
});
```

※ ログイン処理(`action=login`)はスプレッドシートを直接照合しているはずなので、この変更の影響を受けません。

## 変更2: updateWorker で「パスワードが空・未指定なら変更しない」

作業員の更新処理(`action=updateWorker`)で、送られてきた `data` にパスワードが**無い**、または**空文字**の場合は、既存のパスワードを維持するようにしてください。

```javascript
// 例: updateWorker 内、行へ書き込む前に追加
if (!data['パスワード']) {
  delete data['パスワード']; // 既存セルを上書きしない
}
```

※ フロント側(v43.10.1)は「空欄なら既存パスワードを送る」動作なので現行GASのままでも壊れませんが、この変更を入れると通信からもパスワードが消え、完全になります。変更1・2を入れた後は、フロント側も「空欄ならパスワードキー自体を送らない」に切り替えられます(次回改修で対応可能)。

## 変更後の確認手順

1. GASエディタで上記2箇所を修正 → **デプロイ → デプロイを管理 → 新バージョンで更新**
2. 管理画面 → 作業員 → 既存の作業員を開く → パスワード欄が空で表示されること
3. パスワード欄を空のまま「保存」→ その作業員が**従来のパスワードでログインできる**こと(変わっていないこと)
4. パスワード欄に新しい値を入れて「保存」→ **新しいパスワードでログインできる**こと
5. ブラウザの開発者ツール → Application → Local Storage → `genba_admin_cache` に「パスワード」が含まれないこと
